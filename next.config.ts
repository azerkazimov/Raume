import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["onlinepngtools.com", "rickandmortyapi.com", "avatars.githubusercontent.com"],
  }
};

export default nextConfig;
